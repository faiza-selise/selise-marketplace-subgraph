{
  "name": "marketplace-subgraph-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ faiza/marketplace-subgraph-test",
    "create-local": "graph create --node http://localhost:8020/ faiza/marketplace-subgraph-test",
    "remove-local": "graph remove --node http://localhost:8020/ faiza/marketplace-subgraph-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 faiza/marketplace-subgraph-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.2",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
